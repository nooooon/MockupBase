# MockupBase

### Required Components

- Node.js
    - <http://nodejs.org/>


### Setup
#### global install
    $ sudo npm install -g gulp
    
#### local install
    $ npm install
#### run
    $ npm run dev
#### build
    $ npm run build