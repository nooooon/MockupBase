# MockupBase

### Required Components

- Node.js
    - <http://nodejs.org/>


### Setup
#### global install
    $ sudo npm install -g gulp
    
#### local install
    $ npm install
#### run
    $ npm run start
#### development build
    $ npm run dev
#### production build
    $ npm run production